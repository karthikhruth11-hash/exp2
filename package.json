{
  "name": "react-bsc",
  "private": true,
  "scripts": {
    "build": "npm --prefix program-3a-counter run build && npm --prefix program-3b-api-fetch run build && npm --prefix program-3c-props run build && npm --prefix program-3d-froms run build && npm --prefix program-3e-map-rendering run build"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
